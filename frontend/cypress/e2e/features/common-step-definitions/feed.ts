import { Given, Then, When } from "@badeball/cypress-cucumber-preprocessor";


When("a página carregar", () => {
  // Não é necessário realizar nenhuma ação específica, pois a página já foi carregada na etapa Given
});

